spring.application.name=TTproject


## Datasource configuration
# Configure your datasource via environment variables in deployment systems.
# Examples (set as container / host env vars):
#   SPRING_DATASOURCE_URL=jdbc:postgresql://<host>:5432/<db>
#   SPRING_DATASOURCE_USERNAME=youruser
#   SPRING_DATASOURCE_PASSWORD=yourpass
#
# Fallback: if SPRING_DATASOURCE_URL is not set, use the provided PostgreSQL database URL.
# (This can still be overridden via env var in Render or other hosts.)
# Example: postgres://user:pass@host:port/db
spring.datasource.url=${SPRING_DATASOURCE_URL:postgresql://ttproject_user:38aYnuRO1GdpmQWe9QmNwe6x0atKHRxV@dpg-d6g0edrh46gs738loh70-a/ttproject}
# credentials will come from the URL, additional env vars optional
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:}





